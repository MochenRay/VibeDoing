# No.2_RAG与知识库 交接页

> 最近确认时间：2026-06-01

## 当前判断

2026-06-01：个人学习进度已重置。本项目课程材料保留，但 No.2 不再继承此前 B3 讲解断点或任何通过状态。

当前项目仍可作为 `M3 Retrieval / Grounding` 的正式课程入口；进入本项目时应从 `P1 多源数据 vs 多模态` 重新开始。

## 已冻结决策

1. `P1/P2/P3/A1/A2/B1/B2` 先作为稳定主干迁入。
2. `B3` 适用边界保留在课程主线里，但后续要补更系统的案例判断。
3. `C1` 定位为 capstone，而不是一般概念节点。
4. embedding 产品、向量数据库产品、固定 chunk 默认值不直接写进稳定层。
5. `C1` 默认拆成 `product spec + eval plan + fallback / citation design` 三个可交付物。

## 未决问题

1. `B3` 是先做单独判断清单，还是直接嵌进 `C1` 的 pre-read。

## 当前产出

- `project.md`
- `handover.md`
- `state.md`
- `migration-notes.md`
- `curriculum/*.md`
- `refresh/*.md`
- `labs.md` 与 `outcomes.md` 的首版作品模板
- 课程材料保留；个人学习通过记录已清空

## 下一步

1. 待 No.1 完成后，从 `P1 多源数据 vs 多模态` 开始首轮讲解。
2. 进入 B3 时重新生成 `RAG fit memo` 验证题，不沿用旧断点。
