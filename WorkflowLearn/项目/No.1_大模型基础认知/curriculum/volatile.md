# volatile

> 最近确认时间：2026-04-16
> 状态：首版时效内容已归档，官方抽检已完成

## 说明

本文件用于承接时效性强、需要刷新或可能过期的内容。

## 当前条目

- `B1 主流模型对比`
- `初始文档.md` 中的模型价格
- `初始文档.md` 中的上下文窗口和厂商能力快照
- `掌握卡片/B1. 主流模型对比.md` 中的推荐结论
- 中文 Token 经验值如果涉及具体 tokenizer，需要后续按模型族复核
- 任何把“长上下文”直接等同于“可替代 RAG”的结论

## 2026-04-16 官方抽检结果

- OpenAI 当前价格页已经以 `GPT-5.4` 系列为主，不再对应旧项目里的主流模型表。
- Anthropic 当前模型总览已经是 `Claude 4.6 / 4.5` 代际，价格和上下文窗口也与旧记录不同。
- Gemini 当前模型页已经进入 `Gemini 3.1` 预览线与 `Gemini 2.5` 稳定线并存状态，旧推荐结论不能直接沿用。
- Anthropic 与 Gemini 的上下文文档都在强调“context window 是 working memory / short-term memory”，这支持把“长上下文 = 全能方案”留在时效和案例判断层。

## 使用规则

- 所有条目迁入这里时必须补 `last_confirmed`。
- 未经刷新，不得升格为稳定概念。

## 当前判断

- `能力 × 约束 × 性价比` 这种选型框架有保留价值。
- 但任何写死到具体模型名、价格、上下文窗口的结论，当前都不能当正式学习主干。
- 这类内容以后只能作为“学习当期快照”维护，而不是写入长期课程正文。
- 是否该用纯长上下文、RAG 或摘要，也要按当期产品能力和任务结构重算。
